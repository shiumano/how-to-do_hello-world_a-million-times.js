# 貢献について

このプロジェクトへの貢献に興味を持っていただきありがとうございます！

## 新しい実装を追加する方法

### 1. ディレクトリを作成

`src/` ディレクトリ内に、あなたの実装を説明する名前のディレクトリを作成してください。

```bash
mkdir src/[your_method_name]
```

### 2. メインファイルを作成

以下のいずれかのファイル名でソースコードを作成してください：

- `main.js` - JavaScript (ESM)
- `main.cjs` - JavaScript (CommonJS)
- `main.mjs` - JavaScript (ESM, 明示的)
- `main.ts` - TypeScript
- `main.coffee` - CoffeeScript

> [!IMPORTANT]
> **1つのディレクトリには1つのmainファイルと任意でREADME.mdのみ** 配置できます。

### 3. README.mdを作成（推奨）

実装の理解を助けるため、`README.md` を作成することを推奨します：

```bash
# src/[your_method_name]/README.md を作成
```

README.mdには以下を含めてください：

- **実装の概要**（1-2行）
- **仕組みの簡潔な説明**（日本語または英語）
- コードブロックは不要（実際のコードはmain.*を参照）

### 4. ルールに従う

あなたのコードは以下のルールに従う必要があります：

| ルール | 説明 |
|--------|------|
| ❌ `for` / `while` / `forEach` 禁止 | ループ構文は使用できません |
| ❌ 再帰呼び出し禁止 | 関数が自分自身を呼び出すことはできません |
| 📏 1000バイト以内 | ソースコードのサイズ制限 |
| 📁 単一ファイル | `src/[dirname]/main.*` と任意で `README.md` 以外のファイルは禁止 |
| 📝 README.md推奨 | 実装の仕組みを日本語または英語で簡潔に説明すること |

### 5. テストを実行

Pull Requestを送信する前に、ローカルでテストを実行してください：

```bash
# 依存関係のインストール
npm install

# あなたの実装のテスト
npm test [your_method_name]

# 全てのテストを実行
npm run test:all
```

### 6. Pull Requestを作成

テストが通過したら、Pull Requestを作成してください。

## Pull Request のルール

> [!WARNING]
> 以下のルールに違反するPRは自動的にクローズされます。

### 許可される変更

✅ `src/[dirname]/main.[js|cjs|mjs|ts|coffee]` の追加・編集  
✅ `src/[dirname]/README.md` の追加・編集

### 禁止される変更

❌ 上記以外のファイルの追加・編集・削除
❌ 既存の実装ディレクトリの削除

### 自動チェック

Pull Requestは以下の自動チェックを受けます：

1. **ファイルパスの検証** - 許可されたファイルのみが変更されているか
2. **テストの実行** - `npm test [dirname]` が成功するか
3. **削除の検出** - ディレクトリが削除されている場合は警告

## コード例

### JavaScript (x10 method)

```javascript
const x10 = (f) => { f();f();f();f();f();f();f();f();f();f(); };

const main = () => {
    const print = () => console.log("Hello, World!");
    
    x10(() =>
      x10(() =>
        x10(() =>
          x10(() =>
            x10(() =>
              x10(print)
            )
          )
        )
      )
    );
};

main();
```

## 質問・問題

質問や問題がある場合は、[Issues](https://github.com/oto-home/how-to-do_hello-world_a-million-times.js/issues) でお知らせください。
